1、如何引入`BeautifulSoup`模块

```python
from bs4 import Beautiful
```



2、如何通过创建`soup`对象，并通过`BeautifulSoup`模块检查非法标签

```python
soup = BeautifulSoup(content_text,'lxml')

for tag in soup.find_all():
    if tag.name == 'script':
    tag.decompose()	
```



3、循环`soup.find_all()`的`tag`标签，它的`name`属性是什么？

```
存储的是每一个标签对象的名称
```



4、`soup`对象的内容是什么样的？包含哪些内容

```
soup对象是同时包含文本和标签的
```



5、`soup.find_all()`的结果内容是什么样的

```
结果是一个列表，存储的是网页元素中的每一个标签对象
```



6、如果要对包含标签和文章内容的文本，截取出部分文章概要，怎么做

```python
soup = BeautifulSoup(content_text,'html.parser')
# 对文本内容进行切片
html_text = soup.text[:150]
```



7、如何实现简单的`socket`服务端和客户端通信，并一直支持接收并返回消息

服务端：

```python
import socket

server = socket.socket(socket.AF_INET,socket.SOCK_STREAM)
server.setsockopt(socket.SOL_SOCKET,socket.SO_REUSEADDR,1)
server.bind(('127.0.0.1',8080))
server.listen(5)

while True:
    conn, caddr = server.accept()

    while True:
        data = conn.recv(1024)
        conn.send(data.upper())
```

客户端：

```python
import socket

client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
client.connect(('127.0.0.1',8080))

while 1:
    data = input('>>>:')
    if not data:
        continue
    client.send(data.encode('utf-8'))
    data = client.recv(1024)
    print(data)
```



5、在浏览器的什么位置可以查看到服务器反馈的响应体和渲染后页面

6、什么是超文本传输协议，他是基于哪一个应用层的，可以保存用户的状态吗

7、`get`请求和`post`请求在哪获取请求参数

8、`get`和`post`请求有长度限制吗

9、请求头中的`keep-alive`是什么意思

10、`connect-length:16`是什么意思

11、服务器如何返回响应体

12、`post`请求报文中请求头和请求体是如何分隔的

13、响应状态码，1-5开头分表表示什么意思

14、`wsgiref`模块是做什么用的，如何使用

15、浏览器请求的/favicon是做什么用的，应当返回什么数据，每次请求都会请求这个路径吗

16、网页中的图标一般存放在html代码中的什么位置

17、如何使用pymysql模块，如何插入一条数据和插入多条数据，如何提交数据

```python
import pymysql

conn = pymysql.connect(
    host='140.143.132.118',
    port=3306,
    user='xiaoxin',
    password='Nishi458_2',
    db='blog',
    charset='utf8'
)

cursor = conn.cursor()

sql = 'select username from blog_userinfo where username = %s'

ret = cursor.execute(sql,('alex'))
print(cursor.fetchone())
print(cursor.fetchall())

sql = 'insert into user(id, name) values(%s,%s)'

ret = cursor.executemany(sql, [(1,'alex'),(2,'wangxin')])
conn.commit()
```



18、什么是Django的MVC模型，MTV模型
19、如何下载和安装django，使用的什么版本
20、如何通过命令创建django项目

```python
django-admin.py startproject mysites
```

21、如何通过命令创建django应用

```
python3 manage.py startapp blog
```

22、没有templates文件夹怎么办 
23、如何启动django项目，不指定端口号，默认是什么

```
python3 manage.py runserver 8865
```

24、路由分发中的变量是什么，如何编写

```
urlpatterns = [
    
]
```



25、如何实现在Django项目中配置静态文件目录static

```
1、在settings.py中添加:
STATIC_URLS = 'static'
STATIC_FILES = os.path.join(BASE_ORG, 'static')
```



26、在哪引入path、re_path

```
from django.urls import path,re_path,include
```



27、在哪引入render、HttpResponse、redirect

```
from django.shortcuts import render,HttpResponse,redirect
```



28、分组正则表达式的处理逻辑是怎样的

```
（?P<username>.*）
```



29、如何实现将路由分发到应用下的路由分发

```
path('login/', include('app01.urls'))
```



30、提交form表单如果action属性没有填写，表示什么，ajax的url如果没有填写表示什么
31、在视图函数中如何获取get和post请求参数
32、如何在视图函数中知道当前的请求方式？
33、在路由分发中，如何为路径设置别名？在模板中如何调用别名路径

```
path('login', views.login, name = 'Login')

{% url 'Login' %}
```



34、如何在视图函数中反向解析出路由分发中的别名路径

```
from django.urls import reverse
url = reverse('Login')
```



35、queryset是什么数据类型，他是什么样的
36、path方法如何使用转换器，有哪几种

```python
path('login/<str:year>', views.login)
path('login/<int:age>', views.login)
path('login/<slug:username>', views.login)
path('login/<uuid:id>', views.login)
path('login/<path:name>', views.login)
```



37、form表单中action属性为'/index/'表示什么
38、如何通过request打印请求路径，如何打印请求路径+get参数

```python
path('login/<str:year>', views.login)
```



39、如何通过request打印编码方式，该属性可写吗

```
request.encoding
```



40、如何通过request打印所有的请求头信息

```
request.META
```



41、如何通过request打印所有的cookies信息

```
request.COOKIES
```



42、render方法的底层原理是什么

```
打开一个文件后，返回
```



43、在render方法中如何返回数据，有哪两种方式
44、模板过滤器在哪调用，其中default、length、filesizeformat、date、slice、truncatechars、truncatewords、safe、add分别表示什么意思，其中safe过滤器是什么原理
45、模板标签有哪几种
46、如何自定义标签过滤器，如何调用

```
1、在应用下新建templatetags文件夹
2、创建文件如my_filter.py
3、from django import template
4、register = template.Library()
5、定义函数
@register.filter
def multipl(x,y):
	return x*y
6、{{ length | mutipl:2 }}
```



47、如何自定义标签，如何调用

```
1、在应用下新建templatetags文件夹
2、创建文件如my_filter.py
3、from django import template
4、register = template.Library()
5、定义函数
@register.simple_tag
def multipl(x,y):
	return x*y
6、
{% load my_filter %}
{% multipl 2 3 %}
```



48、include模板标签继承和extends模板标签继承如何使用，extends标签有什么使用要求
49、如何在block标签中调用父级盒子中的内容

```
{% block title %}
	{{ block.super }}
{% end block %}
```



50、ORM创建表时，创建表就相当于什么？，需要继承于哪个类

```
from django.db import models
```



51、创建表时，支持哪5种数据类型的列，分别表示什么意思

```
CharField
IntegerField
DateField
EmailField
BooleanField
AutoField
DecimalField
FileField
DateTimeField
TextField
```



52、如何设置为该列为主键列

```
models.AutoField(primary_key = True)
```



53、如何设置该列的最大长度

```
models.AutoField(max_length = 100, primary_key = True)
```



54、在有小数点的数值类型中，如何设置一共有几位数值，小数点有几位

```
models.DecimalField(max_digits = 5, decimal_places = 2)
```



55、Django如何在settings.py中设置数据库连接信息

```
DATABASES = {
    'default': {
        'HOST': '140.143.132.118',
        'PORT': 3306,
        'USER': 'xiaoxin',
        'PASSWORD': '123456',
        'NAME': 'blog',
        'ENGINE': 'django.db.backends.mysql'
    }
}
```



56、连接数据库时，在哪注册当前应用？

```
settings.py中的INSTALLED_APPS = [
    
]
```



57、Django创建表时，都需要做哪4步必做配置，1步可选操作

```python
1、在settings.py的INSTALL_APPS列表中注册应用
2、在settings.py中配置数据库连接信息
3、在项目的__init__.py中:
import pymysql
pymysql.install_as_MySQLdb()
3、执行：
python3 manage.py makemigrations
python3 manage.py migrate
```



58、如何在ORM中新增一条表记录

```
Article.objects.create(username=xxx, password=xxx)
```



59、如何查询该表中所有的记录

```
Article.objects.all()
```



60、如何查询该表中的第一条记录和最后一条记录

```
Article.objects.all().first()
Article.objects.all().last()
```



61、如何在表中过滤数据，有哪两种方法

```python
user = UserInfo.objects.filter(username=username).first()
user = UserInfo.objects.get(username = username)
```



62、如何查询过滤条件以外的数据

```python
article.objects.exclude(title='python从入门到精通')
```



63、如何在ORM语句中升序和降序排序

```python
article.objects.all().order_by('price')
article.objects.all().order_by('-price')
```



64、如何倒序输出所有查询结果

```
article.objects.all().reverse()
```



65、如何统计查询结果的总数

```
article.objects.all().count()
```



66、如何判断当前查询结果是否存在

```
article.objects.all().exists()
```



67、values和values_list的查询结果有什么不同

```python
article.objects.all().values('title') #输出结果是字典的形式，存储列名和列值
article.objects.all().values_list('title') #输出结果是元祖的形式，存储列值
```



68、如何调用去重方法，怎样去重才有意义

```python
#与values配合才有意义
article.objects.all().values('price').distinct()
```



69、什么是链式查询
70、ORM模糊查询__支持哪6种语法，他们都表示什么意思，针对时间列有哪2种模糊查询语法

```python
__gt大于
Article.objects.filter(price__lg=150)

__lt小于
Article.objects.filter(price__lt=100)

__startswith
Aritcle.objects.filter(title__startswith='小')

__contains
__in = []在什么范围之内
Article.objects.filter(price__in = [100,150])

__year,__month
Article.objects.filter(pub_time__year=2011,pub_time__month=9)
```



71、ORM如何删除和修改数据

```python
Article.objects.filter(title='小红帽').update(title='小松鼠')

Article.objects.filter(title='大灰狼').delete()
```



72、如何重定向一个页面

```
from shortcuts import redirect

return redirect('/book/')
```



73、bootstrap提供了哪些将元素和图片左右浮动的方法，实现了什么效果

```
media-right
media-left
pull-right
pull-left
```



74、HTML网页中select标签，传入request.POST中的是一个列表，那么如何通过ORM取值

```
request.POST.get_list()
如果使用request.POST.get()那么得到的是列表最后一个值
```





75、在模板文件中，forloop提供了哪些方法

```python
{{ forloop.conter }} #循环计数
{% if forloop.last %} #循环中最后一次循环
```



76、为什么要将多类信息分散到不同的表结构中

```
解耦
```



77、一对多关系如何创建表关系

```python
models.ForeignKey(to='author', to_field='nid')
```



78、多对多关系如何创建表关系

```python
tags=models.ManyToManyField(
		to=Tag,
         through='Author2AuthorDetail',
         through_fields=('article','tag')
)
```



79、一对一关系如何创建表关系
80、ORM如何实现外键列，外键列生成后是什么样的，如何实现多对多关系，一对一关系
81、一对多关系外键列如何插入数据，有哪两种方式
82、在创建表结构时，如果没有指定多对多关系的through属性，那么会自动生成关系表吗
83、多对多关系表如何插入数据，是根据哪个字段插入的，它提供了哪些添加、移除、清除接口，分别代表什么意思
84、面向对象和双下划线的查询方法是基于mysql中的什么查询
85、按对象查询是由谁调用，基于双下划线是由谁调用
86、面向对象的正向查询按什么，反向查询按什么，调用者是谁
87、基于双下划线的正向查询按什么，反向查询按什么，调用者是谁，在哪使用
88、aggregate 什么意思
89、聚合函数有哪5种，如何引用，如何使用，如何展示结果
90、如何在分组查询时，使用聚合函数，分组查询时，会打印分组的列值吗
91、Book.objects.all().annotate()与Book.objects.annotate()意义一样吗？为什么
92、如何使用F查询，F查询时做什么用途的，有哪两种情况
93、如何调用Q查询，Q查询时做什么用途的
94、jquery的ajax是做什么用的？如何编写，有哪些参数，返回的数据如何处理
95、如何ajax的success中单纯的接收一个视图函返回的字典，如何处理，通过什么方法返回字典就无需此操作了
96、基于form表单如何上传文件，需要添加什么属性，不添加会怎样
97、ajax中如何发送序列化的data数据，调用什么方法
98、如何通过ajax上传文件
99、如何批量的插入数据

100、django如何实现自动分页的效果
101、如何调整分页的显示样式，而不是显示所有的页码呢
102、从哪引入forms组件，它的作用是什么
103、如何创建forms校验类，它的变量名什么要求
104、如何在form校验类中为标签指定label值
105、在get和post请求中，如何分别创建form对象
106、调用什么方法可以在视图函数中通过form对象校验字段，调用该方法时执行了哪些操作，它的返回值是什么
107、校验成功的结果和失败的结果，分别存放在什么位置
108、如何在错误的结果中取出错误的详细信息，如何将其添加到对应的标签
109、如何在HTML网页中通过form对象渲染标签
110、在HTML网页中input标签是如何渲染出来的
111、提交form表单后，如果出现了错误，那么已填写的内容是否会消失
112、在form标签中添加什么属性，会不进行前端的字段校验
113、如何在form标签中显示字段校验的错误，一般在视图函数怎样将错误信息返回给ajax，为什么要使用ajax
114、如何渲染不同的标签类型，如文本类型、密码类型，从哪引入需要的方法。
115、如何在forms校验类中设置标签的属性值，是否需要设置id属性
116、如何在forms校验类中设置指定的错误类型的描述
117、在哪添加局部钩子，它的函数命名是怎样的，它有什么作用
118、在局部钩子中从哪获取经过第一层校验的字段值
119、在局部钩子中如果校验正确返回什么，如果校验错误如何处理，从哪引入
120、在局部钩子中中报出异常后，错误信息被存储到什么位置，如何获取
121、全局钩子的命名是什么，它有什么意义
122、在全局钩子中，对多个字段校验前，应当先判断什么
123、在全局钩子中如果校验正确返回什么，如果校验错误如何处理
124、全局钩子的在errors字典中的key是什么
125、如果在request.POST的请求数据中不存储forms校验类中的字段，那么is_valid()方法的返回值是什么
126、在客户端第一次发送请求后，发送给服务器的cookies是什么样的
127、cookies是由谁设置的
128、每个浏览器之间的cookies是独立的吗？每个服务器之间的cookies是独立的吗？
129、cookie的上限大小是多少
130、服务器最多在客户端存储多少个cookie
131、一个客户端最多存储多少个cookie
132、响应体方法都有哪些，如何创建响应体对象
133、设置cookie的方法是什么，由谁进行设置，最后返回什么
134、在哪可以查看已经存在的cookie信息
135、如果设置了cookie，在其他视图函数中如何使用、验证，跳转
136、登录成功的loing=True的cookie，在其他用户登录后是否会被覆盖？为什么
137、设置cookie时，有哪3个参数设置，如何使用，值的单位是什么
138、cookie是将信息存储在哪，session是将信息存储在哪
139、由谁设置cookie，由谁设置session，如何删除cookie
140、如何设置session，如何读取session
141、在设置session时服务器做了哪3件事？其中session_data是什么样的
143、在读取session时服务器做了哪3件事？
144、通过get方法取出字典值时，如何设置默认值
145、datetime模块如何通过时间格式设置当前时间
146、如何删除指定的session值
147、如何删除当前会话下的所有session信息
148、在删除当前会话的session时，做了哪3步底层操作
-----------------------------------------
149、从哪引入auth模块
150、通过auth模块下的哪个方法可以验证登录的用户名和密码，它的返回值是什么
151、如果验证成功应该执行哪个方法，传入什么参数，执行这个方法的目的是什么
152、如何判断当前用户已登录
153、通过什么命令创建超级用户和普通用户
154、在html模板文件中可以怎样便捷的用哪个变量输出模板变量
155、is_authenticated()表示是否登录，方法是由谁调用，它的返回值是什么，可以用来干什么，
156、auth模块提供了什么样的注销方法，它的底层是如何实现的，需要传入什么参数
157、如何ORM用户接口表没有继承AbstractUser，那么应该引入哪个模块，如何通过它创建用户，为什么用它
158、无论是注册还是修改密码等，都要将request.POST中提交的数据传入校验类校验，校验成功后才执行后续步骤
159、修改密码时，应该先调用什么方法，调用者是谁
160、修改密码时，最后调用什么方法，调用者是谁，如何提交
161、从哪引入登录装饰器，使用前需要添加什么配置项，访问被装饰的页面后，url是什么样的，如何使用
163、django中间件在settings.py中是哪个配置项
164、在中间件中有哪两个方法，他们需要传入什么参数
165、中间件的执行过程是怎样的
166、如果在某一个中间的process_request中添加了return，那么它的执行流程时什么样的
167、process_view方法是在什么时候执行，里面的callback参数表示什么
168、process_exception方法在什么时候执行，需要传入什么参数，如果方法内有return，如何继续执行
169、如何在中间件中实现验证用户是否登录，没有登录跳转到登录页的操作
170、如何计算两个操作之间的时间差
171、如何获取客户端的ip地址
172、ORM中列值唯一如何设置
173、ORM中如何对日期列自动添加当前时间
174、创建用户接口表时，需要继承自哪个表，从哪引入，有什么好处，需要在settings.py中添加什么配置项，应用名+用户接口表名
175、在FileField中，有哪两个属性
176、创建表时添加哪个属性可以在后台管理中，让列名显示为中文
177、创建表时，添加哪个方法可以在打印类时，打印出名称
178、如文章评论数、点赞数等，不用特地生成一张表，直接在文章表中新建列即可，省去了跨表操作
179、BooleanField中如何设置默认值
180、创建表时在一对一、多对多、外键列中需要添加什么属性
181、随机验证码之如何创建背景图片对象，从哪引入
182、随机验证码之如何创建画笔对象，从哪引入
183、随机验证码之如何创建字体对象，从哪引入，需要传入哪些参数
184、如何将随机生成的验证码写到图片上，需要传入哪些参数
185、将随机字符串完整的存放在什么位置进行校验
186、如何获取内存中的文件句柄，从哪引入
187、图片对象如何保存在内存中、保存后如何读取出来
188、如何实现点击图片刷新验证码
189、验证码区分大小写吗
190、如果要在视图函数中给ajax返回字典形式的数据，可以使用哪个方法封装后返回，从哪引入，如何查看已封装的数据内容
191、提交登录请求时，使用ajax提交的请求，不要总想着返回一个页面，要返回一个字典，封装错误信息
192、在ajax中如何使用定时器
193、如果有如获取验证码的工具类，可以在应用下新建utils文件夹，解耦在该文件夹下
194、在html代码中，如何为label标签的for属性找到input标签的id值
195、如何实现点击图片上传头像附件
196、如何实时预览已上传的头像文件
197、如何通过ajax提交带有附件的注册信息，优化方法参考下一题
198、在jquery中如何获取form表单下的所有标签数据，它的结果是什么类型，如何使用
199、如果通过ajax提交的请求，在视图函数中有错误，如何处理
200、bootstrap中将错误标签添加红色边框的类是什么
201、jquery中查找下一个标签的方法是什么
202、jquery中添加或删除类的方法是什么
203、在用户提交注册信息后，在视图函数中调用什么方法创建用户
204、上传文件后，在数据库中avatar字段存储的是什么
205、如果FileField字段指定的upload_to=’avatar/‘不存在怎么办？
206、除了FileField还有什么？用法一样吗
207、如果在调用create_user方法时，有除username、password、email等额外的字段，那么如何传入
203、在PEP8中导入包的顺序是什么
204、为什么要创建media文件夹，在哪创建，用来存储什么
205、在settings.py中需要配置media的什么参数
206、设置了media参数后，与表接口中的哪个参数相匹配
207、如何开放media路径下的文件访问权限，从哪引入需要的serve类
208、如何设置media的url.py路由分发
209、css中的vertical-align是什么作用
210、如何在应用下的admin.py中注册表
211、通过bootstrap的什么类，可以将图片和文字并排显示
212、如何通过bootstarp的类将文字字号减小
213、如何设置settings.py中设置时区
214、如何在date过滤器中显示时分秒
215、css中鼠标hover--a标签时没有下划线的方法是什么
216、如何通过ORM中的extra方法过滤出在2018年11月2号以后的数据
217、如何通过ORM中的extra方法在create_time列中按指定的时间格式过滤出数据
218、如何通过extra方法，按得到的时间格式按月份分组展示数据
219、从哪引入TruncYear、TruncMonth，它们是做什么用的，在哪个方法中引用
220、如何使用TruncYear、TruncMonth进行按月份分组展示书籍数量
221、在settings.py中设置哪项，可以不使用UTC时间
222、数据和html模板相结合时调用什么装饰器，如何使用，如何调用
223、什么是XSS攻击，Django做了什么措施
224、jquery中哪个方法判断目标是否有某个类，返回值是什么
225、jquery中如何使用三元运算符
226、如何在ORM的update中做运算
227、es6提供了什么快捷添加html代码的方式，如何在其内调用变量，怎样添加到代码中
228、字符串的replace方法替换后是否需要重新赋值，为什么
229、jquery中如何获取字符串某个字符的索引
230、jquery中如何切片
231、如何实现锚点
232、jquery中如何设置焦点和失去焦点
233、当queryset数据类型被强制转换为list后，如何通过JsonResponse返回，要添加什么参数
234、django如何实现事务，从哪引入事务，什么是事务
235、django如何发送邮件，在settings.py中有哪些参数
236、如何引入kindeditor编辑器
237、如何判断编辑器的内容为空
238、如何阻止form表单的默认事件
239、如何阻止jquery中的循环
240、如何通过kindeditor编辑器上传文件，需要哪些参数，在视图函数中如何返回编辑器数据
241、Django如何实现类视图，在类视图中有哪些方法，需要继承自什么
242、Django如何使用choices功能









